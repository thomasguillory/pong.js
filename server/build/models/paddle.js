// Generated by CoffeeScript 1.7.1
(function() {
  var Paddle;

  Paddle = (function() {
    Paddle.WIDTH = 2;

    Paddle.HEIGHT = 25;

    Paddle.SPEED = 2;

    function Paddle(options) {
      this.player = options.player;
      this.moveTo(50);
      this.dy = 0;
    }

    Paddle.prototype.moveTo = function(y) {
      this.position = y;
      return this.player.game.emit("player" + this.player.id + ".paddle.position", y);
    };

    Paddle.prototype.update = function() {
      if ((this.dy < 0 && !(this.position < this.constructor.HEIGHT / 2)) || (this.dy > 0 && !(this.position > (100 - this.constructor.HEIGHT / 2)))) {
        return this.moveTo(this.position + this.dy);
      }
    };

    return Paddle;

  })();

  exports.Paddle = Paddle;

}).call(this);
